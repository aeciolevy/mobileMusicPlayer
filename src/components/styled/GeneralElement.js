import styled from 'styled-components';

const footerColor = '#333333';

export const Footer = styled.footer`
    background-color: ${footerColor};
    bottom: 0;
    height: 7rem;
    position: fixed;
    width: 100%;
    z-index: 1500;
`;
